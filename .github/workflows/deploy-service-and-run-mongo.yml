name: Deploy Service and Run Mongo

on:
  workflow_dispatch:
  push:
    branches: [ "master" ]

jobs:
  Publish:
    runs-on: ubuntu-latest  

    env:
       Mongo_Scripts_Path: Projects/Exadel.ReportHub/Exadel.ReportHub.Host/Deployment/Mongo
       Render_Deploy_Hook: https://api.render.com/deploy/srv-cvnri3ngi27c73845qag?key=_XXtGWE8ASs
       Render_Api_Key: rnd_CfZeoLQzhEkjSmrRxeW085bEZYF0
       Render_Service_Id: srv-cvnri3ngi27c73845qag
       ReportHubService_ClientSecret: ${{ secrets.REPORT_HUB_SERVICE_CLIENT_SECRET }}
       Mongo_Connection_String: ${{secrets.MONGO_CONNECTION_STRING}}
       
       Updates: |
         Authority: Auth
         ConnectionStrings__Mongo: String
         EcbConfig__Host: Host1
         ReportHubConfig__Host: Host2
         
    steps:
    - name: Checkout
      uses: actions/checkout@v4
        
    - name: Update Environment variable
      run: |
        IFS=$'\n'
        for udapte in $Updates; do
        key=${udapte%%:*}
        val=${udapte#*:}
        curl -X PUT \
              "https://api.render.com/v1/services/$Render_Service_Id/env-vars/$key" \
              -H "Accept: application/json" \
              -H "Authorization: Bearer $Render_Api_Key" \
              -H "Content-Type: application/json" \
              -d "{\"value\":\"$val\"}"
          done  

    - name: Deploy
      run: curl -X POST "${{ env.Render_Deploy_Hook }}"

    - name: Install MongoSh
      run: |
        sudo apt-get install gnupg
        wget -qO- https://www.mongodb.org/static/pgp/server-7.0.asc | sudo tee /etc/apt/trusted.gpg.d/server-7.0.asc
        echo "deb [ arch=amd64,arm64 ] https://repo.mongodb.org/apt/ubuntu focal/mongodb-org/7.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-7.0.list
        sudo apt-get update
        sudo apt-get install -y mongodb-mongosh

    - name: Load Mongo scripts
      run: |
        for script in "${{ env.Mongo_Scripts_Path }}"/*.js; do
        echo $script
        mongosh "${{ env.Mongo_Connection_String }}" "$script"
        done
    
